# Shell scripts run on the Linux prod box. A CRLF checkout makes the shebang
# `#!/bin/sh\r`, which fails with a bare "not found" that looks nothing like a
# line-ending problem. Force LF regardless of the committer's platform.
*.sh text eol=lf
