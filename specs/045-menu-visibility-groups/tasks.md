---

description: "Task list for 045 — настройки меню выглядят как меню"
---

# Tasks: Настройки меню выглядят как меню

**Input**: Design documents from `/specs/045-menu-visibility-groups/`

**Tests**: обязательны и первыми (принцип II). Оба красных теста падают на
нынешнем коде по существу, и падение идёт в тело PR.

## Format: `[ID] [P?] [Story] Description`

---

## Phase 1: Setup

- [x] T001 Базовый прогон по месту правки: **64 теста в 6 файлах** (`src/components/admin`, `src/components/layout`)

---

## Phase 2: US1 — школа узнаёт свой список настроек (P1) 🎯 MVP

**Goal**: настройки показывают те же пять разделов, что и меню, в том же
порядке и из того же источника.

**Independent Test**: открыть настройки школы — над группами стоят заголовки,
совпадающие с меню.

### Tests ⚠️

- [x] T002 [P] [US1] Тест «секция рисует пять заголовков разделов в порядке меню» в `frontend/src/components/admin/menu-visibility-section.test.tsx` — сегодня заголовков нет вовсе
- [x] T003 [P] [US1] Тест «разделы настроек совпадают с разделами меню по составу и порядку» там же: разделы, выведенные из `MENU_ITEM_KEYS`, сверяются с тем, что отдаёт `buildNavTree` администратору. Тест живёт здесь, а не в `nav-tree.test.ts`, — тот файл правит другая сессия
- [x] T004 [US1] Прогнать тесты, увидеть падение, записать сообщения — идут в тело PR

### Implementation

- [x] T005 [US1] `frontend/src/components/layout/nav-tree.ts`: пять комментариев `// Learning`, `// People`, `// Sessions`, `// Progress`, `// School` заменить полем `group` на каждой из шестнадцати строк `MENU_ITEM_KEYS`. Ничего больше в файле не трогать — `labelKey` не переименовывать и не удалять
- [x] T006 [US1] `frontend/src/components/admin/menu-visibility-section.tsx`: новый компонент с пропами `value` и `onToggle`; порядок разделов **выводится** из `MENU_ITEM_KEYS`, второй константы не заводить; заголовки — ключами `nav.group.*`, они уже есть во всех шести локалях
- [x] T007 [US1] `frontend/src/components/admin/org-settings-form.tsx`: блок видимости заменить вызовом секции; загрузка, состояние и сохранение остаются в форме без изменений

**Checkpoint**: экран настроек читается как меню.

---

## Phase 3: US2 — выключая Support, школа видит цену (P1)

**Goal**: единственный пункт, чьё выключение не очевидно, объясняет себя.

**Independent Test**: выключить Support — появляется пояснение; включить —
исчезает.

### Tests ⚠️

- [x] T008 [P] [US2] Тест «под выключенным Support видно пояснение, под включённым нет, под другими пунктами не бывает никогда» в том же файле теста
- [x] T009 [US2] Прогнать, увидеть падение, записать сообщение

### Implementation

- [x] T010 [US2] Пояснение под выключенным Support в `menu-visibility-section.tsx` — проверкой одного ключа, без общего механизма примечаний
- [x] T011 [US2] Строка `admin.settings.menuSupportOff` во все шесть локалей `frontend/src/lib/i18n/locales/{en,es,ru,tr,de,uk}.ts`. Текст пишется скиллами письма, а не переводится дословно с ветки

---

## Phase 4: Polish

- [x] T012 Полный прогон: **585 passed в 67 файлах**. По месту правки 76 тестов в 7 файлах против базовых 64 в 6 — ровно двенадцать добавленных
- [x] T013 `npx tsc --noEmit`
- [x] T014 Проверить, что храповик i18n доволен: новый `.tsx` зовёт `useTranslation()`, парность шести локалей сходится (`translations.test.ts` и `no-hardcoded-strings.test.ts` в общем прогоне)
- [ ] T015 Браузер: настройки школы — сверить разделы с меню слева на том же экране; выключить Support и увидеть пояснение
- [ ] T016 PR с описанием падений из T004 и T009

---

## Dependencies & Execution Order

- **T005 до T006**: пока раздел не поле, группировать не по чему.
- **T002…T004 до T005**, **T008…T009 до T010** — тест раньше правки.
- T006 до T007: форма зовёт то, что уже существует.
- T011 вместе с T010: строка и её единственное употребление.

## Notes

- Новых строк для заголовков разделов не нужно: `nav.group.learning`,
  `.people`, `.sessions`, `.progress`, `.school` уже есть во всех шести
  локалях. Новая строка ровно одна — про Support.
- С брошенной ветки **не** переносятся `menu-items.ts` и `menu-items.test.ts`:
  второй список тех же ключей, устранённый в specs/040.
- Параллельные сеансы: `nav-tree.ts` и `nav-tree.test.ts` правит другая сессия
  (specs/043). Мой след в первом — одно поле на строку, во втором — ноль строк.
