---

description: "Task list for 041 — вердикт в превью"
---

# Tasks: Кнопка «Check» в превью даёт настоящий вердикт

**Input**: Design documents from `/specs/041-preview-check-verdict/`

**Tests**: обязательны и первыми (принцип II конституции). Оба красных теста
падают на нынешнем коде по существу, и падение идёт в тело PR.

## Format: `[ID] [P?] [Story] Description`

---

## Phase 1: Setup

- [x] T001 Базовый прогон бэкенда: **1325 passed** за 8:48. Работа идёт в отдельном worktree, поэтому под pytest заведена своя база `lms_wt_preview` в том же контейнере — иначе два сеанса делят строки
- [x] T002 Базовый прогон фронтенда снят по месту правки, а не по всему дереву: `src/components/exercises/` — 9 файлов, 102 теста. Полное дерево сверяется в T019

---

## Phase 2: US2 — систему уравнений и стереометрию судит одно правило (P1)

**Goal**: вердикт на пути проверки совпадает с вердиктом на пути сдачи, потому
что вычисляет их одна функция.

**Independent Test**: проверка с верным ответом на систему уравнений отдаёт
`passed=true`; тот же ответ на сдаче отдаёт то же.

### Tests ⚠️

- [x] T003 [P] [US2] Тест «проверка отмечает верный ответ на систему уравнений» в `backend/tests/test_preview_check.py` — сегодня `passed=false`
- [x] T004 [P] [US2] Тест «проверка отмечает верный ответ на стереометрию» там же — сегодня `passed=false`
- [x] T005 [P] [US2] Тест «вердикт проверки совпадает с вердиктом сдачи на одном и том же ответе» там же — для обоих типов, и для верного ответа, и для неверного
- [x] T006 [P] [US2] Тест «система с неразрешимыми уравнениями даёт 400 про настройки, а не отрицательный вердикт» там же
- [x] T007 [US2] Прогнать `backend/tests/test_preview_check.py`, увидеть падение, записать сообщения — идут в тело PR

### Implementation

- [x] T008 [US2] `backend/app/submissions/service.py`: две ветки в `grade_interactive_detail` — `math_system` решает систему через `solve_linear_system` и сверяет `solutions_match`, `stereometry` считает величину через `solids.compute` и сверяет с допуском. Правило переносится из функций сдачи слово в слово, `BadRequestError` про поломанные настройки — тоже
- [x] T009 [US2] `backend/app/exercises/service.py`: `_submit_math_system` и `_submit_stereometry` перестают вычислять вердикт сами и зовут `grade_interactive_detail`. Сохранение решения, начисление опыта, замер времени и нормализация ответа остаются на месте
- [x] T010 [US2] Убедиться, что `backend/app/exercises/router.py` не потребовал ни одной правки — если потребовал, значит правило разъехалось и решение Р1 сделано неверно

**Checkpoint**: сервер судит два типа одинаково с обеих сторон.

---

## Phase 3: US4 — тестовый прогон не пишет ничего (P1)

**Goal**: закрыть дыру до того, как её откроет починка номера.

**Independent Test**: превью задания с кодом показывает надпись «только
просмотр» и не даёт отправить решение.

- [x] T011 [US4] `frontend/src/components/exercises/exercise-renderer.tsx`: `code_challenge` в `SELF_PERSISTING_TYPES` — к шести типам, которые сохраняют сами себя

**Checkpoint**: номер задания можно отдавать превью, ничего не запишется.

---

## Phase 4: US1 — «Check» даёт вердикт (P1) 🎯 MVP

**Goal**: превью спрашивает сервер про то задание, которое редактируется.

**Independent Test**: в редакторе задания ответить верно и нажать «Check» —
виден положительный вердикт.

### Tests ⚠️

- [x] T012 [P] [US1] Тест «превью отправляет проверку по адресу с номером задания» в `frontend/src/components/exercises/live-preview.test.tsx` — сегодня в адресе `undefined`
- [x] T013 [US1] Прогнать тест, увидеть падение, записать сообщение — идёт в тело PR

### Implementation

- [x] T014 [US1] `frontend/src/components/exercises/live-preview.tsx`: обязательный проп `exerciseId`, поле `id` в объекте, который уходит в `ExercisePreview`
- [x] T015 [P] [US1] `frontend/src/app/(admin)/admin/content-library/[exerciseId]/page.tsx`: передать `exerciseId`
- [x] T016 [P] [US1] `frontend/src/components/exercises/exercise-config-panel.tsx`: передать `exerciseId` — он уже обязательный проп панели

**Checkpoint**: кнопка выдаёт вердикт на всех типах с серверной разметкой.

---

## Phase 5: US3 — отказ проверки виден (P2)

**Goal**: там, где вердикта нет, учитель видит причину, а не пустоту.

**Independent Test**: проверка отвечает ошибкой — на экране строка о
недоступности вердикта, вердикта нет.

- [x] T017 [US3] `frontend/src/components/exercises/v2-exercise-live.tsx`: тестовый прогон ловит отказ проверки и показывает `admin.exercisePreview.verdictUnavailable`, как это давно делает соседний путь в `exercise-renderer.tsx`. Вердикта при отказе не рисовать
- [x] T017a [US3] Дописать в `live-preview.test.tsx` случай «проверка отказала — виден текст о недоступности, вердикта нет»

---

## Phase 6: Polish

- [ ] T018 `cd backend && python -m pytest -q` — сверить с базовым числом из T001
- [x] T019 Фронтенд: **565 passed** в 66 файлах (+2 к базовому — новый файл), `npx tsc --noEmit` чист
- [x] T020 `ruff check` и `ruff format --check` по трём изменённым файлам чисты. По всему `backend/` местная ruff 0.15.10 находит 14 ошибок и 157 неотформатированных файлов — всё это было до правки, чужое не трогаем
- [ ] T021 Браузер на проде: `system-SY004` — ответить «нет решений», нажать «Check», увидеть положительный вердикт; ответить «одно решение» — отрицательный. Тот самый экран, на котором дефект найден
- [ ] T022 Браузер: открыть превью задания с кодом — «только просмотр», отправки нет
- [ ] T023 Проверить по базе прода, что тестовые прогоны из T021 и T022 не создали ни одного решения
- [ ] T024 PR с описанием падений из T007 и T013

---

## Dependencies & Execution Order

- **T008 до T014.** Пока сервер не умеет отмечать два типа, номер в превью
  меняет молчание на ложное «неверно» — хуже, чем было.
- **T011 до T014.** Пока номера нет, задание с кодом безопасно случайно.
  Обратный порядок оставляет окно, в котором прогон учителя пишет сдачу.
- **T003…T007 до T008**, **T012…T013 до T014** — тест раньше правки.
- T015 и T016 параллельны друг другу, оба после T014.
- Phase 5 — в любой момент после T013.

## Notes

- Новых пользовательских строк нет: `admin.exercisePreview.verdictUnavailable`
  и `admin.exercisePreview.visualOnly` уже есть во всех шести локалях.
  Храповик i18n не срабатывает, парность не трогаем.
- Миграций нет, схема не меняется.
- `live-preview.test.tsx` заводится по образцу `v2-exercise-live.test.tsx`:
  `@/lib/api-client` и `@/lib/i18n/context` подменяются через `vi.mock`,
  user-event в проекте нет — клики через `fireEvent`.
