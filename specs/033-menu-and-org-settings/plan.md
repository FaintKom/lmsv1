# Implementation Plan: Меню короче, настройки школы достижимы

**Branch**: `feat/menu-and-org-settings` | **Date**: 2026-08-20 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `specs/033-menu-and-org-settings/spec.md`

## Summary

Боковое меню суперадмина — 22 пункта плоским списком, свернуть его нельзя, а
настройки чужой школы суперадмину недостижимы. Работа сводит меню к пяти
сворачиваемым категориям и «Дашборду», даёт рельс с логотипом и названием
школы, объединяет записи со встречами на одной странице и открывает экран
настроек любой организации.

Подход: одно состояние оформления в Zustand с сохранением под ключом
`sidebar-collapsed`, который страница `/cookies` уже описывает; вкладки на
`/admin/meetings` по образцу `/admin/journal`; форма настроек выносится в
компонент с обязательным `orgId` и получает второго вызывающего. Бэкенд не
трогаем — `GET`/`PUT /admin/organizations/{org_id}` уже принимает `settings` и
уже пускает суперадмина в любую школу.

Полное обоснование каждого решения — [research.md](./research.md).

## Technical Context

**Language/Version**: TypeScript 5, строгий режим; React 19; Next.js 16 (App Router)

**Primary Dependencies**: Zustand (+ `zustand/middleware` для `persist`),
TanStack Query, Tailwind 4 с токенами Lively, `lucide-react`

**Storage**: Local storage браузера, ключ `sidebar-collapsed`. Базы данных
задача не касается: миграций нет, схема не меняется.

**Testing**: Vitest — юнит-тесты на сборку меню и на редирект; Playwright —
сквозные проверки в `frontend/e2e/journeys/`

**Target Platform**: браузер; рабочий стол от 768 px и телефон

**Project Type**: веб-приложение, изменения только во `frontend/`

**Performance Goals**: сворачивание и раскрытие 150–300 мс, при
`prefers-reduced-motion` — мгновенно

**Constraints**:

- Ратчет i18n: новый `.tsx` обязан звать `useTranslation()` или попасть в
  `i18n-allowlist.ts`, а список разрешено только сокращать. Значит — переводить.
- Ключ обязан быть во всех шести локалях (`en`, `es`, `ru`, `tr`, `de`, `uk`),
  иначе падает `translations.test.ts`.
- `tsc --noEmit` — шлюз CI. Новых предупреждений ESLint не добавлять, чужие не
  чинить.
- Каждый новый маршрут утяжеляет `next build`, а он идёт на прод-боксе.
  Добавляется ровно один маршрут; редирект уходит в `next.config.ts`.

**Scale/Scope**: 1 новый маршрут, 1 новый стор, 2 новых компонента, 1 новый
модуль API, около 8 правок существующих файлов, 6 локалей

## Constitution Check

*GATE: пройден до Phase 0, перепроверен после Phase 1.*

| Принцип | Как соблюдается |
|---|---|
| **I. Изоляция арендаторов** | Экран настроек чужой школы — единственное место задачи, где `org_id` приходит из адреса. Проверку делает сервер: `admin.role != super_admin and admin.org_id != org_id` → `NotFoundError`, то есть 404, а не 403 — ровно как требует принцип. Своей проверки на клиенте не изобретаем: «интерфейс не показывает кнопку» контролем не считается. Тест на изоляцию открывается положительным контролем — сначала показываем, что суперадмин чужую школу правит, и только потом, что администратор школы получает 404. Без этого «чужая школа отдаёт 404» зеленеет и на несуществующем экране. |
| **II. Тест, способный упасть** | Каждая проверка сперва демонстрируется красной против нынешнего поведения. Для меню это просто: сегодня категорий нет, кнопки сворачивания нет, `/admin/recordings` — отдельная страница. Демонстрация падения указывается в теле PR. Запрещены `status in (200, 403)` и проверки вида «в ответе есть нужные ключи». |
| **III. Ответ судит сервер** | Задача не касается проверки ответов. |
| **IV. Продукт и документация говорят одно** | Страница `/cookies` уже обещает ключ `sidebar-collapsed`. Работа делает обещание правдой — это FR-004 и SC-008, а не побочный эффект. |
| **V. Наименьшее изменение** | Удаляем `search-bar.tsx` целиком вместо того, чтобы прятать. Переиспользуем `tooltip.tsx`, блок `redirects()` в `next.config.ts` и разметку вкладок из `/admin/journal`. Общий компонент `Tabs` не заводим: второго вызывающего у него не будет. Форму настроек выносим ровно в тот момент, когда у неё появляется второй вызывающий. |

Нарушений нет; раздел «Complexity Tracking» не заполняется.

## Project Structure

### Documentation (this feature)

```text
specs/033-menu-and-org-settings/
├── plan.md              # этот файл
├── spec.md
├── research.md          # Phase 0
├── data-model.md        # Phase 1
├── quickstart.md        # Phase 1
├── contracts/
│   └── ui.md            # Phase 1 — контракт интерфейса, не HTTP
├── checklists/
│   └── requirements.md
└── tasks.md             # Phase 2, создаёт /speckit-tasks
```

### Source Code (repository root)

```text
frontend/
├── next.config.ts                                  # +1 редирект
└── src/
    ├── stores/
    │   └── ui-store.ts                             # НОВЫЙ
    ├── lib/
    │   ├── api/
    │   │   └── organizations.ts                    # НОВЫЙ
    │   └── i18n/locales/{en,es,ru,tr,de,uk}.ts     # +ключи, −ключи search.*
    ├── components/
    │   ├── layout/
    │   │   ├── sidebar.tsx                         # категории, рельс, бренд
    │   │   ├── nav-tree.ts                         # НОВЫЙ — описание групп
    │   │   └── search-bar.tsx                      # УДАЛЯЕТСЯ
    │   ├── ui/tooltip.tsx                          # +показ по фокусу
    │   └── admin/
    │       └── org-settings-form.tsx               # НОВЫЙ — вынос формы
    └── app/
        ├── (admin)/
        │   ├── layout.tsx                          # читает состояние из стора
        │   └── admin/
        │       ├── courses/page.tsx                # +фильтр
        │       ├── meetings/page.tsx               # вкладки, +записи
        │       ├── recordings/page.tsx             # УДАЛЯЕТСЯ (редирект)
        │       ├── settings/page.tsx               # рендерит вынесенную форму
        │       └── organizations/
        │           ├── page.tsx                    # карточка ведёт на экран
        │           └── [orgId]/page.tsx            # НОВЫЙ — единственный маршрут
        ├── (dashboard)/layout.tsx                  # читает состояние из стора
        └── cookies/page.tsx                        # сверить, что описание совпало
```

**Structure Decision**: правки живут во `frontend/src/`, потому что бэкенд
задачу уже поддерживает. Каталоги реальные, из текущего дерева. Единственный
новый маршрут — `admin/organizations/[orgId]`; `admin/recordings` не заменяется
маршрутом-заглушкой, а уходит в `redirects()` конфига, чтобы не платить за него
сборкой на прод-боксе.

## Phase 2 preview

`/speckit-tasks` разложит работу на пять срезов по историям спеки. Порядок
диктует один файл: `sidebar.tsx` трогают истории 1, 2 и 5, поэтому они идут
подряд и одной веткой, а не параллельно.

Слияние в `main` — это деплой. Ветка одна, PR ожидается один; если он вырастет
до неудобного размера, делится по границе историй, и тогда мержится по одному,
с ожиданием завершения предыдущего деплоя.
