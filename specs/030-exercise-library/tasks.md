---

description: "Task list for 030 — библиотека заданий"
---

# Tasks: Библиотека заданий, в которую можно класть

**Input**: Design documents from `/specs/030-exercise-library/`

**Tests**: обязательны и первыми. Три из них падают на нынешней схеме и
нынешней загрузке.

---

## Phase 1: Setup

- [ ] T001 Поднять postgres (`docker compose up -d db`), снять базовый прогон `cd backend && python -m pytest -q`
- [x] T002 ~~Дождаться мержа PR #416~~ — смержен 2026-08-21, как и #411 и #415; редактор урока и плеер свободны
- [x] T002a Перемерить прод перед стартом. Замер 2026-08-21: 147 заданий, 66 уроков, 135 показаны блоком, **10 привязаны к уроку без блока**, заимствований 0, висящих ссылок 0. Листать только по `page` — `offset` эндпоинт игнорирует и всегда отдаёт первую страницу, из-за чего первый замер дал ложный ноль. Число хвостовых меняется само: редактор урока дописывает им блоки при открытии урока, поэтому перед миграцией мерить ещё раз

---

## Phase 2: Foundational

- [x] T003 Прогнать `/speckit-analyze` — сделано; нашёл переделку загрузки и источник `org_id`, оба внесены в план
- [ ] T004 Создать `backend/tests/test_exercise_library.py`: организация, курс, модуль, урок; вторая организация со своим курсом и уроком

---

## Phase 3: US1 — задание живёт без урока (P1) 🎯 MVP

### Tests ⚠️

- [ ] T005 [P] [US1] Тест «задание создаётся без урока и попадает в список школы» в `backend/tests/test_exercise_library.py`
- [ ] T006 [P] [US1] Тест «школа берётся у вызывающего: учитель не создаст задание в чужой организации» в том же файле
- [ ] T007 [P] [US1] Тест «список отличает поставленное от непоставленного» в том же файле
- [ ] T008 [US1] Прогнать `backend/tests/test_exercise_library.py`, увидеть падение, записать сообщения — идут в тело PR

### Implementation

- [ ] T009 [US1] Миграция, часть 1, в `backend/alembic/versions/`: `exercises.lesson_id` допускает NULL, внешний ключ на `ON DELETE SET NULL`; перед `ALTER` проверять состояние
- [ ] T010 [US1] `backend/app/exercises/models.py`: `lesson_id` необязателен
- [ ] T011 [US1] `backend/app/courses/models.py`: снять `cascade="all, delete-orphan"` у `Lesson.exercises`
- [ ] T012 [US1] `backend/app/exercises/service.py`: `create_exercise` берёт `org_id` у вызывающего, когда урока нет; урок, если передан, по-прежнему проверяется на принадлежность школе
- [ ] T013 [US1] `backend/app/exercises/schemas.py`: `lesson_id` необязателен на входе; в ответе списка признак «поставлено»

---

## Phase 4: US3 + US4 — задание переживает всё, кроме своего удаления (P1)

### Tests ⚠️

- [ ] T014 [P] [US4] Тест «блок удалён — задание осталось в библиотеке, даже без решений и без других мест» в `backend/tests/test_exercise_library.py`
- [ ] T015 [P] [US4] Тест «урок с тремя заданиями удалён — три задания остались» в том же файле
- [ ] T016 [P] [US3] Тест «задание в двух уроках разных курсов переживает удаление одного» в том же файле
- [ ] T017 [P] [US5] Тест «удаление задания убирает блок из обоих уроков» в том же файле — правки не ждём, `remove_block_references` уже обходит все уроки организации

### Implementation

- [ ] T018 [US4] Убедиться, что удаление блока в редакторе перестало удалять задание: сегодня удаляет (проверено живьём 2026-08-20). Найти вызов в `frontend/src/app/(admin)/admin/lessons/[lessonId]/edit/page.tsx` и убрать — **если файл занят другим сеансом, задача уходит в PR 2**

---

## Phase 5: US2 + US6 — урок читает блоки, ничего не пропадает (P1)

### Tests ⚠️

- [ ] T019 [P] [US2] Тест «урок отдаёт задание, на которое ссылается блок, даже если `lesson_id` у него чужой» в `backend/tests/test_exercise_library.py`
- [ ] T020 [P] [US2] Тест «ученик не получает `solution_code` через эту загрузку» в том же файле
- [ ] T021 [P] [US6] Тест миграции данных: задание с уроком и без блока получает блок в конце последней страницы — в `backend/tests/test_exercise_library_migration.py`

### Tests — изоляция школ ⚠️

- [ ] T019a [P] Тест «сохранение урока с блоком, называющим задание чужой школы, отвергается» в `backend/tests/test_exercise_library.py` — FR-012, падает на нынешнем коде: контент пишется без проверок
- [ ] T019b [P] Тест-контроль «сохранение урока с блоком своего задания проходит» в том же файле — без него первый зелёный и на запрете вообще всех блоков
- [ ] T019c [P] Тест «поиск по библиотеке не отдаёт чужое, но отдаёт своё» в том же файле

### Implementation

- [ ] T022 [US2] `backend/app/exercises/service.py`: `get_exercises_by_lesson` читает `exercise_id` из блоков страниц урока вместо `lesson_id`; фильтр по организации **читателя** остаётся; ответ по-прежнему идёт через `_for_reader`
- [ ] T022a `backend/app/courses/service.py`: `update_lesson` проверяет, что каждый `exercise_id` в блоках принадлежит организации вызывающего, и отвергает чужой. Единственное место, где номер задания приходит из запроса
- [ ] T023 [US6] Миграция, часть 2: каждому заданию с уроком и без блока дописать блок в конец последней страницы; рерун-безопасно. На 2026-08-21 таких заданий 10, среди них задание с решениями учеников — без переноса шесть уроков теряют содержимое в момент выката
- [ ] T023a [US6] Убрать `withDetached` и его тесты из `frontend/src/app/(admin)/admin/courses/[courseId]/edit/lesson-elements.ts` — после T022 загрузка читает блоки, дорисовывать нечего (заведено в specs/037)
- [ ] T023b Переписать `frontend/src/lib/lessons/add-exercise.ts` под новую модель: создание без `lesson_id`, постановка блоком отдельным шагом. Единственное место, где страница курса заводит задание
- [ ] T024 [US6] Плеер `frontend/src/app/(dashboard)/courses/[courseId]/lessons/[lessonId]/page.tsx`: убрать хвостовую секцию «упражнения урока» — только после T023: на 2026-08-21 она показывает ученику те самые 10 заданий

---

## Phase 6: US1 (фронт) — создание в каталоге

- [ ] T025 Создание в `frontend/src/app/(admin)/admin/content-library/page.tsx` с теми же группами типов, что и в уроке
- [ ] T026 Признак «не поставлено» в строке списка там же
- [ ] T027 [P] Новые строки во все шесть локалей `frontend/src/lib/i18n/locales/{en,es,ru,tr,de,uk}.ts`

---

## Phase 7: Polish

- [ ] T028 `cd backend && python -m pytest -q`, сверить с базовым числом из T001
- [ ] T029 `cd frontend && npm test` и `npx tsc --noEmit`
- [ ] T030 `ruff check` и `ruff format` по `backend/`
- [ ] T031 Браузер: создать задание в каталоге; открыть урок из тех шести и убедиться, что задание на месте
- [ ] T032 PR с описанием падения из T008
- [ ] T033 **Перед мержем**: свежий `pg_dump` прода
- [ ] T034 Мерж, деплой; проверка в проде: 121 задание на месте, 0 висящих ссылок, те 10 показаны блоками

---

## Phase 8: PR 2 — поиск в блоке, когда освободится редактор урока

- [ ] T035 Поиск по библиотеке в блоке-задании: по названию, номеру и типу; создание нового — продолжение того же места
- [ ] T036 Тест «поиск не отдаёт задания чужой школы»
- [ ] T037 Сквозная проверка: одно задание в уроках двух курсов, решено в первом — во втором видно решение

---

## Dependencies & Execution Order

- **T002 до всего**: PR #416 правит тот же плеер
- **Phase 3 → Phase 4**: отвязка проверяется только когда NULL разрешён
- **Phase 5 после Phase 3**: загрузка по блокам бессмысленна, пока задание обязано иметь урок
- **T023 до T024**: сначала перенести те 10 в блоки, потом убирать секцию, которая их показывала. Обратный порядок — шесть уроков без содержимого
- **Phase 8 отдельным PR**: файл занят двумя сеансами

## Notes

Порядок в проде: бэкап, мерж, деплой, проверка. Миграция данных трогает
только уроки тех десяти заданий; остальные 111 не переписываются.
